import '@testing-library/cypress/add-commands'

describe('QATEST-4835 - Testing Verify/reject POA - Mobile view', () => {
  beforeEach(() => {
    cy.c_login()
  })

  it('Should show verified page when the POA is verified', () => {})
})
